import { describe, it, expect } from "vitest";

// A trivial test to prove the pipeline runs. Replace with real component tests
// (for example, render App with @testing-library/react and assert on the output).
describe("smoke", () => {
  it("runs the test pipeline", () => {
    expect(1 + 1).toBe(2);
  });
});
